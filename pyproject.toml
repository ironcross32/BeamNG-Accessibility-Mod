[project]
name = "beam"
version = "0.1.0"
description = "A screen reader mod for BeamNG.drive"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "aiohttp>=3.12.15",
    "asyncio>=4.0.0",
    "keyboard>=0.13.5",
    "mouse>=0.7.1",
    "mss>=9.0.1",
    "numpy>=2.3.3",
    "ordered-set>=4.1.0",
    # Pinned, not floated: prismatoid is pre-1.0 and its Context/Backend API
    # still moves between minor releases.
    "prismatoid==0.17.3",
    "sounddevice>=0.5.2",
    "wxpython>=4.2.3",
    "zstandard>=0.25.0",
]

[dependency-groups]
dev = [
    "ipython>=9.10.1",
    "nuitka>=4.0",
    "h5py>=3.8.0",  # build-time only: bake_hrtf.py extracts the SOFA file into .npz
]
